# alx_travel_app_0x03 – Background Tasks with Celery

This project configures Celery with RabbitMQ to send booking confirmation emails asynchronously.

## Setup

Install dependencies:
```bash
pip install celery django-celery-results django-celery-beat
